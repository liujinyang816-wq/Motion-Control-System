/**
 ******************************************************************************
 * @file           : bsp_timer.h
 * @brief          : 板级定时器驱动头文件
 ******************************************************************************
 */
#ifndef __BSP_TIMER_H
#define __BSP_TIMER_H

#include "osal.h"

/* ============================================================================
 * 函数声明
 * ============================================================================ */

void     bsp_InitTimer(void);                  /**< 初始化定时器（预留） */
uint32_t bsp_GetTickCount(void);               /**< 获取系统运行时间戳 */
void     TIM2_Int_Init(uint32_t arr, uint16_t psc); /**< TIM2 定时器中断初始化 */
void     Delay_us(uint32_t time);              /**< 微秒延时（软件循环） */
void     Delay_ms(uint32_t time);              /**< 毫秒延时（基于 HAL_Delay） */

#endif
